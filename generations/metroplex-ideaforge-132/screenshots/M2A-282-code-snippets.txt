================================================================================
CRITICAL SECURITY FIX CODE SNIPPETS
================================================================================

1. PATH TRAVERSAL PROTECTION (src/skillhub/core/cache.py)
----------------------------------------------------------

BEFORE:
    for file_path, content in files.items():
        target_path = skill_version_dir / file_path
        target_path.parent.mkdir(parents=True, exist_ok=True)
        target_path.write_bytes(content)

AFTER:
    for file_path, content in files.items():
        # Validate file size
        if len(content) > MAX_FILE_SIZE:
            raise ValueError(f"File too large: {file_path} ({len(content)} bytes, max {MAX_FILE_SIZE})")

        target_path = (skill_version_dir / file_path).resolve()
        # Validate path is within skill_version_dir
        if not target_path.is_relative_to(skill_version_dir.resolve()):
            raise ValueError(f"Path traversal attempt detected: {file_path}")
        target_path.parent.mkdir(parents=True, exist_ok=True)
        target_path.write_bytes(content)

2. PACKAGE SIZE VALIDATION (src/skillhub/core/cache.py)
--------------------------------------------------------

ADDED AT START OF cache_skill():
    # Validate total package size
    total_size = sum(len(content) for content in files.values())
    if total_size > MAX_PACKAGE_SIZE:
        raise ValueError(f"Package too large: {total_size} bytes (max {MAX_PACKAGE_SIZE})")

3. VERSION STRING INJECTION FIX (src/skillhub/core/cache.py)
-------------------------------------------------------------

BEFORE:
    skill_info["versions"].sort(key=lambda v: [int(x) for x in v.split(".")])

AFTER:
    def _version_key(v):
        try:
            return [int(x) for x in v.split(".")]
        except (ValueError, AttributeError):
            return [0, 0, 0]
    skill_info["versions"].sort(key=_version_key)

4. PACKAGE SPEC VALIDATION (src/skillhub/cli/commands/install.py)
------------------------------------------------------------------

BEFORE:
    def parse_package_spec(spec: str) -> tuple:
        if "@" in spec:
            parts = spec.split("@", 1)
            return parts[0], parts[1]
        return spec, None

AFTER:
    def parse_package_spec(spec: str) -> tuple:
        if not spec or not spec.strip():
            raise ValueError("Package spec cannot be empty")
        if "@" in spec:
            parts = spec.split("@", 1)
            if not parts[0] or not parts[1]:
                raise ValueError("Invalid package spec format")
            return parts[0].strip(), parts[1].strip()
        return spec.strip(), None

5. SYMLINK FILTERING (src/skillhub/cli/commands/install.py)
------------------------------------------------------------

BEFORE:
    for file_path in temp_path.rglob("*"):
        if file_path.is_file():
            rel_path = file_path.relative_to(temp_path)
            source_files[str(rel_path)] = file_path.read_bytes()

AFTER:
    for file_path in temp_path.rglob("*"):
        if file_path.is_file() and not file_path.is_symlink():
            rel_path = file_path.relative_to(temp_path)
            source_files[str(rel_path)] = file_path.read_bytes()

================================================================================
